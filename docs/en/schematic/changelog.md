# Changelog

## 2022042501

- Added `GROUP` grouping control element.

## 2022030701

- Redesigned `OBJ` format.
- `OBJ` added `BLOB` reference mode.

## 2022030201

- Undisplayed `ATTR` is marked by either X or Y being `null`.

## 2021111801

- Redesigned `INSTANCE` attribute override document format.

## 2020102301

- `COMPONENT` no longer embeds the device inside the primitive; it is stored as an attribute.

## 2020101401

- `SYMBOL` primitive adjusted to `COMPONENT` primitive, introducing Device and symbol_uuid override mechanism.

## 2020092801

- Initial version notes.
