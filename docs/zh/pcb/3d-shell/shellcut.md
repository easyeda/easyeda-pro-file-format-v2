# SHELLCUT 外壳挖槽区域（废弃）

没有通过 `CONNECT` 与 `CREASE` 关联的 `SHELLCUT`，则垂直进行顶层、底层外壳的挖槽。

## 格式

```json
["SHELLCUT", "e35", 0, 50, 10, 10, 复杂多边形, 0]
```

## 字段说明

1. `SHELLCUT`：外壳挖槽区域标识。
2. 图元编号。
3. 分组编号：`0` 不分组，非 `0` 为组标志，相同组标志的为一组。
4. 层编号。
5. 深度。
6. 线宽。
7. 参考复杂多边形。
8. 是否锁定。
