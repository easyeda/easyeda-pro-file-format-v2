# GROUP 分组配置

通过分组配置，给每个组一个名称。如果无名称，由 EDA 决定如何显示默认组名。

## 格式

```json
["GROUP", 2, "ABCD"]
```

## 字段说明

1. `GROUP`：分组配置标识。
2. 分组编号。
3. 名称。

## 示例

```json
["GROUP", 2, "ABCD"]
["GROUP", 5, "中文名称"]
```
