# 设计规则模板

设计规则模板逻辑上有两种理解方式，PCB 自行根据需要选择：

1. 作为其他设计规则的基版，其他设计规则是对模板的覆盖。
2. 与其他设计规则互斥，有模板其他设计规则只是暂存，不产生任何效果。
3. 作为来源于哪个模板的标识，不影响后续实际规则的效力（当前采用的方案）。

## 格式

```json
["RULE_TEMPLATE", "JLCPCB Capability(High Frequency Board)"]
```

## 字段说明

1. `RULE_TEMPLATE`：设计规则模板标识。
2. 模板名称。
