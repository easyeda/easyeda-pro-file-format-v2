# 设计规则

## 格式

```json
["RULE", "Safe Clearance", "通用", 1, {unit: "mm", xxxxx}]
```

## 字段说明

1. `RULE`：设计规则标识。
2. 规则类型：EDA 自己决定。
3. 规则名称。
4. 规则状态：`0` 普通规则、`1` 默认规则、`2` 停用规则。
5. 规则内容：EDA 自己决定。

同一规则类型设计规则出现的顺序，需要和【规则管理】左侧树的顺序一致。
