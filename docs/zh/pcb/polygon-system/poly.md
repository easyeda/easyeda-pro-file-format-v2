# POLY 折线

折线和 `LINE`、`ARC` 区别较小，但是其具有保持绘制时“连续的一条线”的概念，以和 `REGION`、`FILL`、`POUR` 互转。

## 格式

```json
["POLY", "e100", 0, "GND", 1, 0.5, 单多边形, 0]
```

## 字段说明

1. `POLY`：折线标识。
2. 图元编号。
3. 分组编号：`0` 不分组，非 `0` 为组标志，相同组标志的为一组。
4. 网络。
5. 层。
6. 线宽。
7. 请参考单多边形章节。
8. 是否锁定。
