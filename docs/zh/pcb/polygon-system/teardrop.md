# TEARDROP 泪滴

泪滴不可选中，不可直接操作，当关联的任意图元发生变化时，EDA 应让其自动消失。

## 格式

```json
["TEARDROP", "e200", "GND", 3, 简单多边形, 0]
```

## 字段说明

1. `TEARDROP`：泪滴标识。
2. 编号。
3. 网络。
4. 层。
5. 简单多边形。
6. 分组编号：`0` 不分组，非 `0` 为组标志，相同组标志的为一组。
