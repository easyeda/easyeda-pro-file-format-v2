# 格式版本

原理图相关文档类型包括：

- `SCH`：原理图文档
- `SYMBOL`：符号类型文档

当前版本示例：

```json
["DOCTYPE", "SCH", "1.1"]
["DOCTYPE", "SYMBOL", "1.1"]
```

## 字段说明

1. `DOCTYPE`：文档类型标识。
2. 文档类型：`SCH` 或 `SYMBOL`。
3. 文档格式版本号：每次发布设计格式变动的版本前都要调整版本号。

文件格式带上版本号后，可以通过版本号做前向兼容。
