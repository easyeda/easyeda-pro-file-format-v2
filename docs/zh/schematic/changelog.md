# 修订日志

## 2022042501

- 添加 `GROUP` 分组控制元素。

## 2022030701

- 重新设计 `OBJ` 格式。
- `OBJ` 添加 `BLOB` 引用模式。

## 2022030201

- 未显示过的 `ATTR`，通过 X/Y 任一为 `null` 标记。

## 2021111801

- 重新设计 `INSTANCE` 属性覆盖文档格式。

## 2020102301

- `COMPONENT` 图元不再将 device 放入图元，而是作为其属性存在。

## 2020101401

- `SYMBOL` 图元调整为 `COMPONENT` 图元，引入 Device 和 symbol_uuid 属性覆盖机制。

## 2020092801

- 初始版本相关说明。
